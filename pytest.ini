[pytest]
# Only collect tests under ./tests (skip older_files)
testpaths = tests
# Make src/ importable as a package root
pythonpath = src
# (Optional) belt-and-suspenders: don't recurse into these dirs
norecursedirs = .venv venv build dist older_files sandbox
