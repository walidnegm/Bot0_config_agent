{
  "name": "summarize_config_files",
  "description": "“Summarize a specified list of config files (no directory scanning) for keys and possible secrets.",
  "import_path": "bot0_config_agent.tools.tool_scripts.summarize_config_files.summarize_config_files",
  "parameters": {
    "type": "object",
    "properties": {
      "files": {
        "type": "array",
        "items": { "type": "string" },
        "description": "List of file paths to summarize."
      }
    },
    "required": ["files"]
  },
  "input_model": "SummarizeCongifFilesInput",
  "output_model": "SummarizeConfigFilesResult"
}
