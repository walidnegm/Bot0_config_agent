{
    "name": "scan_config_directory",
    "description": "Recursively scans a directory for config-like files, extracting top-level keys and identifying potential secrets based on keywords like 'key', 'token', and 'password'.",
    "import_path": "bot0_config_agent.tools.tool_scripts.scan_config_directory.scan_config_directory",
    "parameters": {
        "type": "object",
        "properties": {
            "dir": {
                "type": "string",
                "description": "The base directory to scan. Defaults to the current working directory."
            }
        },
        "required": []
    },
    "input_model": "ScanConfigDirectoryInput",
    "output_model": "ScanConfigDirectoryResult"
}

